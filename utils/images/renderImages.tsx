// import { Key } from "react";
// import ImageItem from "./imageItem";

const RenderImages = () => {
  // if (images.length > 0) {
  //   return images.map((image: any, idx: Key | null | undefined) => {
  //     return <ImageItem fields={image} />;
  //   });
  // }
  // return <ImageItem fields={images} />;
  return null;
};

export default RenderImages;
